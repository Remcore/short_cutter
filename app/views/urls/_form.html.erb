<%= form_with(model: url, local: true) do |form| %>
  <% if url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
        <% url.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :short_url %>
    <%= form.text_field :short_url %>
  </div>

  <div class="field">
    <%= form.label :full_url %>
    <%= form.text_field :full_url %>
  </div>

  <div class="field">
    <%= form.label :open_count %>
    <%= form.number_field :open_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
